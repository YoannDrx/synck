# ============================================
# Environment Setup Instructions
# ============================================
# 1. Copy this file to .env.development and .env.production
# 2. Run: pnpm db:setup-env (to auto-fill Neon URLs)
# 3. Add your API keys manually (RESEND_API_KEY, BLOB_READ_WRITE_TOKEN)
# 4. In local development, this file is not used (use .env.local or direct commands)

# Database (Neon PostgreSQL)
# Get from: neonctl connection-string [branch-name] --pooled / --direct
DATABASE_URL="postgresql://neondb_.."
DIRECT_URL="postgresql://neondb_..."

# Site Configuration
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Email (Resend)
RESEND_API_KEY="re_..."

# Vercel Blob (Optional, for image uploads)
BLOB_READ_WRITE_TOKEN="vercel_blob_..."
